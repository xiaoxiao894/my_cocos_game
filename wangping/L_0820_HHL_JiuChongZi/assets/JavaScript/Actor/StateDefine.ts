export enum StateDefine {
    Attack = "attack",
    Idle = "idle",
    Walk = "walk_f",
    Walk_attack = "walk-attack",
    Die = "Die",
    IdleP = "idleP",
    AttackP = "attackP",
    WalkP = "walkP",
}

export enum MonsterStateDefine {
    Attack = "attack01",
    Walk = "walk_f",
    Die = "Die",
    Idle = "Idle"
}

export enum PeopleEnum {
    Idle = "idle",
    Walk = "walk_f"
}

export enum PartnerEnum {
    Attack = "attack",
    Idle = "idle",
    Walk = "walk_f",
    Walk_attack = "walk-attack",
    Die = "Die",
}
