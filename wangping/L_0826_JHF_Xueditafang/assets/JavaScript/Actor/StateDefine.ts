export enum StateDefine {
    Attack = "MQ_Attack",
    Idle = "idle",
    Walk = "MQ_Run",
    Walk_attack = "MQ_Run-Attack",
    Die = "Die",
}

export enum MinionStateEnum {
    Attack = "attack",
    Idle = "idle",
    Walk = "walk_f"
}

export enum MonsterStateEnum {
    // Attack = "attack01",
    // Idle = "idle",
    // Die = "die",
    // Walk = "walk_f",
    Attack = "Attack",
    Idle = "idle",
    Die = "Die",
    Walk = "Run"
}

export enum PartnerAttackEnum {
    Attack = "attack"
}